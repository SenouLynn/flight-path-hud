name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  javascript:
    name: JavaScript and TypeScript
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Test MAVLink bridge and real-SITL fixtures
        run: pnpm test:bridge

      - name: Test GCS core
        run: pnpm test:gcs-core

      - name: Test browser GCS
        run: pnpm test:gcs

      - name: Test shared HUD UI
        run: pnpm test:hud-ui

      - name: Test HUD application
        run: pnpm test:hud

      - name: Lint HUD application
        run: pnpm lint:hud

      - name: Build browser GCS
        run: pnpm build:gcs

      - name: Build HUD application
        run: pnpm build:hud

  esp32-native:
    name: ESP32 native logic
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run native C++ tests
        run: make -C apps/esp32 test
